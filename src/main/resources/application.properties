# Enable embedded in-memory Artemis broker
spring.artemis.mode=embedded
spring.artemis.embedded.enabled=true
spring.artemis.embedded.persistent=false

# Explicitly route JMS connections to the embedded broker via in-memory transport
spring.artemis.broker-url=vm://0

# Set default queue name
spring.jms.template.default-destination=school-shoutouts-queue